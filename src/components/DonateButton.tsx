"use client";

import { useState } from "react";

export default function DonateButton({
    recipient,
    amount,
    callback,
}: {
    recipient: string;
    amount: number;
    callback: (...args: any[]) => void;
}) {
    return <></>
}
